org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Disabled: on Windows, when the project drive (e.g. F:) differs from the
# Pub Cache drive (e.g. C:), Kotlin's incremental compiler cache can throw
# "Cannot invoke java.io.File.exists() because parent is null" while closing
# its cache (a known cross-drive path bug). Incremental compilation is only
# a build-speed optimization, so it's safe to disable.
kotlin.incremental=false
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
